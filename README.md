# Project

This repository contains a minimal Python project scaffold with a sample `app` package, tests, and a GitHub Actions workflow.

Quick start:

```powershell
python -m venv .venv
.venv\Scripts\Activate.ps1
pip install -r requirements.txt
pytest
```
